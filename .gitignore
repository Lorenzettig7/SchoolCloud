# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Environment files
.env
*.pem
*.key

# Node / dist builds (if portal source is included)
node_modules/
dist/
build/
.cache/
